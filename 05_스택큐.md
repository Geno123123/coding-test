# 스택 / 큐 (Stack / Queue)

## 1. Stack

스택은 **LIFO(Last In First Out)** 구조다.

> 마지막에 들어간 것이 먼저 나온다.

```text
push 3
push 5
push 7

[3, 5, 7]
       ↑
      pop
       ↓
       7
```

Python에서는 `list`를 사용하면 된다.

```python
stack = []

stack.append(10)
stack.append(20)
stack.append(30)

x = stack.pop()  # 30
```

---

## 2. Stack 대표 문제

- 괄호 검사
- 후위 표기식
- DFS
- 뒤에서부터 처리
- 이전 상태로 되돌리기
- 단조 스택

### 괄호 검사 예시

```python
stack = []

for x in s:
    if x == "(":
        stack.append(x)
    else:
        if not stack:
            return False
        stack.pop()

return not stack
```

---

## 3. Queue

큐는 **FIFO(First In First Out)** 구조다.

> 먼저 들어간 것이 먼저 나온다.

Python에서는 `deque`를 사용하는 것이 좋다.

```python
from collections import deque

q = deque()

q.append(10)
q.append(20)
q.append(30)

x = q.popleft()  # 10
```

---

## 4. 왜 list로 큐를 만들면 안 좋은가?

```python
arr.pop(0)
```

리스트의 앞쪽 원소를 제거하면 뒤의 원소들을 이동시켜야 해서 비효율적이다.

따라서:

```python
from collections import deque

q = deque()
q.append(x)
q.popleft()
```

를 사용한다.

---

## 5. BFS와 Queue

BFS의 핵심은:

```python
from collections import deque

q = deque([start])

while q:
    node = q.popleft()

    for nxt in graph[node]:
        q.append(nxt)
```

**가까운 것부터 처리해야 하기 때문에 Queue가 잘 맞는다.**

---

## 6. Stack과 DFS

DFS는 스택을 이용해 구현할 수 있다.

```python
stack = [start]

while stack:
    node = stack.pop()

    for nxt in graph[node]:
        stack.append(nxt)
```

재귀 함수 자체도 호출 스택을 사용하므로 DFS를 재귀로 구현할 수 있다.

---

## 7. 대표적인 문제

### Stack

- 올바른 괄호
- 같은 숫자는 싫어
- 주식가격
- 탑
- 괄호 제거
- DFS

### Queue

- 다리를 지나는 트럭
- 프로세스
- 카드2
- BFS
- 미로 탐색

### 기억할 것

```text
Stack → LIFO → list.append / list.pop
Queue → FIFO → deque.append / deque.popleft
BFS → Queue
DFS → Stack / 재귀
```
