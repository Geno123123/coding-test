# 탐욕법 (Greedy)

## 1. 핵심 개념

탐욕법(Greedy)은 **현재 단계에서 가장 좋은 선택을 반복해서 전체 최적해를 만드는 방법**이다.

핵심 질문:

> "지금 이 순간 어떤 선택을 하는 것이 가장 유리한가?"

단, 단순히 좋아 보이는 선택을 한다고 해서 항상 정답이 되는 것은 아니다.

**탐욕적 선택이 전체 최적해로 이어진다는 근거가 필요하다.**

---

## 2. 대표적인 예: 거스름돈

동전이 `[500, 100, 50, 10]`이고 1260원을 거슬러 준다면 큰 동전부터 사용한다.

```python
coins = [500, 100, 50, 10]
money = 1260
count = 0

for coin in coins:
    count += money // coin
    money %= coin
```

---

## 3. 가장 대표적인 패턴: 정렬 후 선택

많은 Greedy 문제는 **정렬을 먼저 하는 것**에서 시작한다.

예: 회의실 배정

```python
meetings.sort(key=lambda x: x[1])

end = 0
count = 0

for start, finish in meetings:
    if start >= end:
        count += 1
        end = finish
```

종료 시간이 빠른 회의를 먼저 선택하는 것이 핵심이다.

---

## 4. Greedy 문제를 판단하는 법

다음 질문을 해본다.

1. 현재 가장 좋은 선택이 명확한가?
2. 그 선택을 한 뒤 남은 문제도 같은 방식으로 해결할 수 있는가?
3. 현재 선택을 나중에 되돌릴 필요가 없는가?
4. 정렬을 기준으로 앞에서부터 선택하면 되는가?

특히 **"최대한 많이", "최소 개수", "가장 빨리", "가장 큰 이익"** 같은 표현이 등장하면 Greedy 가능성을 생각한다.

---

## 5. Greedy와 DP 구분

### Greedy

```text
현재 최선 선택
    ↓
다음 최선 선택
    ↓
정답
```

### DP

```text
여러 선택지를 고려
    ↓
부분 문제 결과 저장
    ↓
최적해 구성
```

Greedy는 빠르고 간단할 수 있지만 적용 조건이 더 까다롭다.

---

## 6. 자주 나오는 Greedy 문제

- 거스름돈
- 회의실 배정
- 큰 수 만들기
- 구명보트
- 체육복
- 섬 연결하기
- 최소/최대 비용 문제

### 기억할 것

```text
Greedy = 지금 가장 좋은 선택
        ↓
정렬 + 앞에서부터 선택
```

하지만 **"그렇게 선택해도 최적해가 된다는 이유"**가 핵심이다.
