# 해시 (Hash)

## 1. 핵심 개념

Python에서는 해시를 주로 **`dict`와 `set`**으로 사용한다.

해시의 핵심 장점은 **값을 빠르게 찾는 것**이다.

```python
d = {}
d["apple"] = 3

print(d["apple"])
```

---

## 2. Dictionary

```python
person = {
    "name": "Kim",
    "age": 20
}
```

### 추가/수정

```python
person["age"] = 21
person["job"] = "developer"
```

### 조회

```python
person["name"]
```

키가 없을 수 있다면:

```python
person.get("phone")
```

기본값도 지정 가능하다.

```python
person.get("phone", "없음")
```

---

## 3. 키 존재 여부

```python
if key in d:
    ...
```

이게 가장 기본적인 패턴이다.

잘못된 방식:

```python
if d[key]:
    ...
```

키가 없으면 `KeyError`가 발생한다.

---

## 4. 빈도수 세기

코테에서 매우 자주 사용한다.

```python
count = {}

for x in arr:
    if x in count:
        count[x] += 1
    else:
        count[x] = 1
```

더 간단하게:

```python
from collections import Counter

count = Counter(arr)
```

또는:

```python
from collections import defaultdict

count = defaultdict(int)

for x in arr:
    count[x] += 1
```

---

## 5. Set

중복을 제거하거나 존재 여부를 빠르게 확인할 때 사용한다.

```python
s = set()

s.add(10)
s.add(20)
s.add(10)

print(s)  # {10, 20}
```

### 존재 여부

```python
if 10 in s:
    ...
```

### 중복 제거

```python
arr = [1, 2, 2, 3, 3]
arr = list(set(arr))
```

단, `set`은 순서를 보장하는 용도로 사용하면 안 된다.

---

## 6. 딕셔너리 순회

```python
for key in d:
    print(key)

for key, value in d.items():
    print(key, value)

for value in d.values():
    print(value)
```

---

## 7. 대표적인 해시 문제

### 완주하지 못한 선수

```python
from collections import Counter

count = Counter(participant)

for x in completion:
    count[x] -= 1

for x in count:
    if count[x] > 0:
        return x
```

### 두 배열의 교집합/중복 확인

`set`을 활용할 수 있다.

---

## 8. 해시를 써야 하는 순간

```text
"이 값이 존재하는지 반복해서 확인"
"각 값이 몇 번 등장하는지"
"이름 → 점수"
"ID → 정보"
"값 → 인덱스"
```

이런 문제라면 `dict`/`set`을 먼저 떠올린다.

### 기억할 것

```text
빠른 조회 → dict / set
키-값 저장 → dict
중복 제거/존재 확인 → set
빈도수 → Counter / defaultdict
```
