# 동적 계획법 (Dynamic Programming, DP)

## 1. 핵심 개념

DP는 **큰 문제를 작은 부분 문제로 나누고, 이미 계산한 결과를 저장해서 다시 사용하는 방법**이다.

DP의 핵심은 두 가지다.

1. **부분 문제가 반복된다.**
2. **작은 문제의 답으로 큰 문제의 답을 만들 수 있다.**

---

## 2. 가장 기본적인 예: 피보나치

```text
F(n) = F(n-1) + F(n-2)
```

단순 재귀는 같은 계산을 반복한다.

```python
def fib(n):
    if n <= 1:
        return n
    return fib(n - 1) + fib(n - 2)
```

DP를 사용하면 계산 결과를 저장할 수 있다.

---

## 3. Bottom-Up

작은 값부터 계산한다.

```python
def fib(n):
    if n <= 1:
        return n

    dp = [0] * (n + 1)
    dp[1] = 1

    for i in range(2, n + 1):
        dp[i] = dp[i - 1] + dp[i - 2]

    return dp[n]
```

여기서:

```text
dp[i] = i번째 문제의 정답
```

이라고 정의한다.

---

## 4. Top-Down + Memoization

재귀를 사용하면서 계산 결과를 저장한다.

```python
dp = [-1] * (n + 1)

def fib(x):
    if x <= 1:
        return x

    if dp[x] != -1:
        return dp[x]

    dp[x] = fib(x - 1) + fib(x - 2)
    return dp[x]
```

---

## 5. DP 문제를 만났을 때

다음 순서로 생각한다.

### ① 상태 정의

"dp[i]가 무엇을 의미하지?"

예:

```text
dp[i] = i번째 계단까지 올라오는 방법의 수
```

### ② 점화식

"이전 상태에서 어떻게 현재 상태가 만들어지지?"

예:

```text
dp[i] = dp[i-1] + dp[i-2]
```

### ③ 초기값

```text
dp[0] = ...
dp[1] = ...
```

을 결정한다.

### ④ 순서

작은 문제부터 큰 문제를 계산한다.

---

## 6. 1차원 DP 대표 문제: 계단 오르기

한 번에 1칸 또는 2칸 올라갈 수 있다면:

```python
dp[i] = dp[i - 1] + dp[i - 2]
```

예:

```python
n = 5
dp = [0] * (n + 1)

dp[1] = 1
dp[2] = 2

for i in range(3, n + 1):
    dp[i] = dp[i - 1] + dp[i - 2]

print(dp[n])
```

---

## 7. 2차원 DP

행/열 두 가지 상태가 필요하면 2차원 배열을 사용한다.

```python
dp = [[0] * (m + 1) for _ in range(n + 1)]
```

대표적으로:

- 배낭 문제
- 격자 경로
- LCS
- 문자열 편집
- 여러 조건이 있는 최적화 문제

등에서 사용한다.

---

## 8. DP인지 판단하는 질문

문제를 읽고 다음을 확인한다.

```text
① 같은 작은 문제를 반복해서 풀고 있는가?
② 이전 결과를 저장하면 계산을 줄일 수 있는가?
③ 현재 답이 이전 상태들의 답으로 표현되는가?
```

특히 다음 표현이 나오면 DP 가능성을 생각한다.

- 경우의 수
- 최대/최소
- 몇 가지 방법
- i번째까지의 최댓값
- 이전 선택의 결과가 다음 선택에 영향을 줌

---

## 9. Greedy와 DP 비교

### Greedy

```text
현재 최선
   ↓
다음 최선
   ↓
정답
```

### DP

```text
여러 이전 상태
   ↓
현재 상태의 최적값
   ↓
다음 상태
```

Greedy는 선택을 한 뒤 과거 선택을 다시 고려하지 않는 경우가 많고, DP는 여러 상태의 결과를 저장해 비교한다.

---

## 10. DP에서 가장 중요한 것

DP는 코드를 외우는 것보다 **`dp[i]`의 의미를 정의하는 능력**이 중요하다.

예:

```text
dp[i] = i까지 고려했을 때 얻을 수 있는 최대 점수
```

이 정의가 명확해지면 점화식이 자연스럽게 나온다.

### 기억할 것

```text
DP
 ├─ 상태 정의
 ├─ 점화식
 ├─ 초기값
 └─ 작은 문제 → 큰 문제
```
