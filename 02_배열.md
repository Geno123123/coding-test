# 배열 (Array)

## 1. 핵심 개념

Python에서는 일반적으로 `list`를 배열처럼 사용한다.

```python
arr = [10, 20, 30, 40]
```

인덱스는 `0`부터 시작한다.

```python
arr[0]  # 10
arr[2]  # 30
```

---

## 2. 주요 사용법

### 생성

```python
arr = []
arr = [1, 2, 3]
arr = [0] * 5
```

### 추가

```python
arr.append(10)
```

뒤에 추가한다.

### 삭제

```python
arr.pop()       # 마지막 원소 제거
arr.pop(0)      # 0번 인덱스 제거
```

`pop(0)`은 앞의 원소를 제거하므로 반복적으로 사용하면 비효율적이다. 큐가 필요하면 `deque`를 사용한다.

### 정렬

```python
arr.sort()                  # 오름차순, 원본 변경
arr.sort(reverse=True)      # 내림차순

new_arr = sorted(arr)       # 새 리스트 반환
```

---

## 3. 인덱싱과 슬라이싱

```python
arr = [10, 20, 30, 40, 50]

arr[1]      # 20
arr[-1]     # 50
arr[1:4]    # [20, 30, 40]
arr[:3]     # [10, 20, 30]
arr[::2]    # [10, 30, 50]
```

---

## 4. 배열 순회

```python
for x in arr:
    print(x)
```

인덱스가 필요하면:

```python
for i in range(len(arr)):
    print(i, arr[i])
```

둘 다 필요하면:

```python
for i, x in enumerate(arr):
    print(i, x)
```

두 배열을 동시에 순회하려면:

```python
for a, b in zip(arr1, arr2):
    ...
```

---

## 5. 2차원 배열

```python
n = 3
m = 4

arr = [[0] * m for _ in range(n)]
```

주의:

```python
# 피해야 함
arr = [[0] * m] * n
```

위 방식은 같은 내부 리스트를 공유하기 때문에 한 행을 수정하면 다른 행도 같이 바뀔 수 있다.

---

## 6. 자주 사용하는 패턴

### 최댓값/최솟값

```python
max(arr)
min(arr)
```

### 합

```python
sum(arr)
```

### 개수

```python
len(arr)
```

### 특정 값의 개수

```python
arr.count(3)
```

### 포함 여부

```python
if 3 in arr:
    ...
```

단, `in`은 리스트에서 선형 탐색이므로 자주 검사해야 한다면 `set`이나 `dict`를 고려한다.

---

## 7. 코테에서 배열과 함께 자주 쓰는 것

- 정렬
- 투 포인터
- 슬라이딩 윈도우
- 누적합
- 이진 탐색
- `enumerate`
- `zip`

### 기억할 것

```text
배열 → Python list
순차 접근 → for
인덱스 필요 → enumerate
두 배열 동시 순회 → zip
앞/뒤 삽입·삭제가 중요 → deque 고려
```
