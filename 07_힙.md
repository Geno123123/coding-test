# 힙 (Heap)

## 1. 핵심 개념

힙(Heap)은 **우선순위가 높은 원소를 빠르게 꺼내기 위한 자료구조**다.

Python 코테에서는 직접 구현하기보다 `heapq` 모듈을 사용한다.

기본적으로 `heapq`는 **최소 힙(min heap)**이다.

> 가장 작은 값이 항상 빠르게 꺼낼 수 있는 위치에 있다.

---

## 2. 기본 사용법

```python
import heapq

heap = []

heapq.heappush(heap, 5)
heapq.heappush(heap, 2)
heapq.heappush(heap, 8)
heapq.heappush(heap, 1)

print(heapq.heappop(heap))  # 1
print(heapq.heappop(heap))  # 2
```

핵심 연산:

```python
heapq.heappush(heap, value)  # 삽입
heapq.heappop(heap)          # 최솟값 제거 후 반환
heap[0]                      # 최솟값 확인
```

---

## 3. 리스트를 힙으로 만들기

이미 리스트가 있다면:

```python
import heapq

arr = [5, 2, 8, 1, 3]
heapq.heapify(arr)

print(heapq.heappop(arr))  # 1
```

`heapify()`는 기존 리스트를 힙 구조로 만든다.

---

## 4. 최대 힙

Python의 `heapq`는 최소 힙만 제공한다.

최대값부터 꺼내고 싶다면 **음수로 저장**한다.

```python
import heapq

heap = []

for x in [5, 2, 8, 1]:
    heapq.heappush(heap, -x)

print(-heapq.heappop(heap))  # 8
print(-heapq.heappop(heap))  # 5
```

---

## 5. 왜 정렬 대신 힙을 사용하는가?

매번 가장 작은 값을 찾아야 하는 상황에서 매번 전체 정렬을 하는 것보다 힙이 효율적이다.

```text
정렬
→ 전체를 다시 정렬해야 함

힙
→ 필요한 최솟값/최댓값을 빠르게 꺼냄
```

대표적인 상황:

> "현재 가장 작은 것부터 처리한다."

> "현재 가장 우선순위가 높은 것을 꺼낸다."

---

## 6. 튜플을 힙에 넣기

힙에는 튜플도 넣을 수 있다.

```python
heap = []

heapq.heappush(heap, (3, "A"))
heapq.heappush(heap, (1, "B"))
heapq.heappush(heap, (2, "C"))

print(heapq.heappop(heap))
# (1, 'B')
```

첫 번째 값부터 비교한다.

그래서 `(거리, 노드)` 형태가 **다익스트라**에서 매우 자주 등장한다.

```python
heapq.heappush(heap, (distance, node))
```

---

## 7. 우선순위 큐

힙은 Python에서 우선순위 큐를 구현할 때 가장 많이 사용한다.

```python
import heapq

pq = []

heapq.heappush(pq, (2, "일반"))
heapq.heappush(pq, (1, "긴급"))
heapq.heappush(pq, (3, "낮음"))

priority, task = heapq.heappop(pq)
print(priority, task)
# 1 긴급
```

숫자가 작을수록 우선순위가 높다고 가정한 예다.

---

## 8. 대표적인 코테 문제

- 더 맵게
- 디스크 컨트롤러
- 이중 우선순위 큐
- 최소 힙
- 최대 힙
- 다익스트라
- 최소 신장 트리(Prim)

---

## 9. 다익스트라와 힙

가중치가 양수인 그래프에서 최단 거리를 구할 때 자주 사용한다.

```python
import heapq

INF = float('inf')
dist = [INF] * n
dist[start] = 0

heap = [(0, start)]

while heap:
    distance, node = heapq.heappop(heap)

    if distance > dist[node]:
        continue

    for nxt, weight in graph[node]:
        new_dist = distance + weight

        if new_dist < dist[nxt]:
            dist[nxt] = new_dist
            heapq.heappush(heap, (new_dist, nxt))
```

핵심은:

```text
그래프 + 최단거리
       ↓
현재 가장 짧은 거리부터 선택
       ↓
heapq
```

---

## 10. 시간 복잡도

일반적으로:

```text
heappush → O(log N)
heappop  → O(log N)
heap[0]  → O(1)
heapify  → O(N)
```

---

## 11. 힙 문제를 판단하는 방법

문제에서 다음과 같은 상황이 반복되면 힙을 생각한다.

- 가장 작은 값을 계속 꺼낸다.
- 가장 큰 값을 계속 꺼낸다.
- 우선순위가 높은 작업부터 처리한다.
- 현재까지의 최소/최대값을 반복적으로 선택한다.
- 다익스트라를 구현한다.

### 기억할 것

```text
최솟값 반복 선택 → heapq
최댓값 반복 선택 → 음수 + heapq
우선순위 큐      → heapq
다익스트라       → 그래프 + heapq
```
