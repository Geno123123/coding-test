# 그래프 (Graph)

## 1. 핵심 개념

그래프는 **정점(Vertex/Node)**과 **간선(Edge)**으로 관계를 표현하는 자료구조다.

- 정점: 도시, 사람, 컴퓨터, 상태 등
- 간선: 정점 사이의 연결 관계
- 방향 그래프: `A → B`
- 무방향 그래프: `A — B`
- 가중치 그래프: 간선마다 비용/거리 등이 존재
- 인접 정점: 특정 정점과 직접 연결된 정점

코테에서는 그래프 자체보다 **그래프를 어떻게 저장하고 탐색하는지**가 중요하다.

---

## 2. 인접 리스트

가장 자주 사용하는 표현 방법.

```python
n = 5
graph = [[] for _ in range(n)]

graph[0].append(1)
graph[1].append(0)  # 무방향

graph[0].append(2)
graph[2].append(0)
```

결과:

```text
0 ─ 1
│
2
```

`graph[0]`은 `[1, 2]`가 된다.

### 입력으로 간선이 주어지는 경우

```python
for _ in range(m):
    a, b = map(int, input().split())
    graph[a].append(b)
    graph[b].append(a)
```

방향 그래프라면 `graph[b].append(a)`는 넣지 않는다.

---

## 3. BFS

BFS(Breadth-First Search)는 **가까운 정점부터 넓게 탐색**한다.

Python에서는 `deque`를 큐로 사용한다.

```python
from collections import deque

q = deque([start])
visited = [False] * n
visited[start] = True

while q:
    node = q.popleft()

    for nxt in graph[node]:
        if not visited[nxt]:
            visited[nxt] = True
            q.append(nxt)
```

### BFS의 대표 용도

- 최단 거리(가중치가 동일한 그래프)
- 미로 탐색
- 몇 단계 만에 도달하는지
- 레벨별 탐색

### 최단거리 BFS

```python
from collections import deque

dist = [-1] * n
dist[start] = 0
q = deque([start])

while q:
    node = q.popleft()

    for nxt in graph[node]:
        if dist[nxt] == -1:
            dist[nxt] = dist[node] + 1
            q.append(nxt)
```

---

## 4. DFS

DFS(Depth-First Search)는 **한 방향으로 깊게 들어간 후 돌아오는 방식**이다.

### 재귀 DFS

```python
def dfs(node):
    visited[node] = True

    for nxt in graph[node]:
        if not visited[nxt]:
            dfs(nxt)
```

### 스택 DFS

```python
stack = [start]
visited[start] = True

while stack:
    node = stack.pop()

    for nxt in graph[node]:
        if not visited[nxt]:
            visited[nxt] = True
            stack.append(nxt)
```

---

## 5. 코테에서 자주 나오는 그래프 유형

- 연결 요소 개수
- BFS/DFS 탐색
- 미로/최단거리
- 사이클 탐지
- 위상 정렬
- 다익스트라
- 최소 신장 트리
- 유니온 파인드

### 기억할 것

```text
그래프
 ├─ 인접 리스트
 ├─ BFS → deque
 ├─ DFS → stack/재귀
 └─ 가중치 최단거리 → 다익스트라 + heapq
```
